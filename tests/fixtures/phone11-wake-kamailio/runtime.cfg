#!KAMAILIO
#!define WITH_PHONE11_WAKE_CANDIDATE
#!define PHONE11_WAKE_PILOT_URI "sip:1001@test.invalid"
#!define PHONE11_WAKE_API "http://fixture:8080/api/phone11/wake"
#!define PHONE11_WAKE_SECRET "synthetic-runtime-test-only"
#!define PHONE11_WAKE_FLAG 30
listen=udp:0.0.0.0:15060
children=2
shm_mem_size=32
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "textops.so"
loadmodule "siputils.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "jansson.so"
loadmodule "dialog.so"
loadmodule "rtpengine.so"
modparam("rtpengine", "rtpengine_sock", "udp:fixture:22222")
modparam("usrloc", "db_mode", 0)
include_file "/candidate/modules.inc"
request_route {
    if (is_method("OPTIONS")) { sl_send_reply("200", "OK"); exit; }
    if (is_method("CANCEL")) {
        if (t_lookup_cancel("1") && isflagset(PHONE11_WAKE_FLAG)) { route(PHONE11_WAKE_CANCEL); }
        if (t_check_trans()) { route(RELAY); }
        exit;
    }
    if (is_method("REGISTER")) { save("location"); exit; }
    if (has_totag()) {
        if (is_known_dlg() && loose_route()) {
            if (is_method("BYE") && $dlg_var(phone11_wake) == "1") { route(PHONE11_WAKE_END); }
            route(RELAY);
        }
        exit;
    }
    if (is_method("INVITE")) {
        record_route(); dlg_manage(); route(PHONE11_WAKE_START); exit;
    }
    exit;
}
# Synthetic NG responder records allocations/deletes; no real media endpoints.
route[PHONE11_INBOUND_OFFER] {
    if (!rtpengine_offer()) { t_reply("503", "Synthetic media unavailable"); exit; }
    if ($ci =~ "^fixture-media-relay-error-") {
        $du = "sip:synthetic@127.0.0.1:9;transport=tls";
    }
    return;
}
route[RELAY] { if (!t_relay()) { sl_reply_error(); } exit; }
include_file "/candidate/routes.inc"
