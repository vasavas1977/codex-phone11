import { FeatureUnavailable } from "@/components/feature-unavailable";
export default function Screen() {
  return <FeatureUnavailable title="Conference calls are not available yet" description="Use Phone to make an individual voice call, or Team Chat for a group text conversation." />;
}
