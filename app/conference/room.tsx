import { FeatureUnavailable } from "@/components/feature-unavailable";
export default function Screen() {
  return <FeatureUnavailable title="Conference calls are not available yet" description="No conference is connected here. Return to Phone for an individual voice call." />;
}
