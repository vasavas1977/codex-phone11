import { FeatureUnavailable } from "@/components/feature-unavailable";
export default function Screen() {
  return <FeatureUnavailable title="Notification center is not available yet" description="Open Team Chat to check saved messages and unread counts. Message alerts while the app is closed are not connected yet." />;
}
