import { FeatureUnavailable } from "@/components/feature-unavailable";
export default function Screen() { return <FeatureUnavailable title="Call transfer is not available yet" description="Your current voice call stays connected. Return to the call controls to continue talking or end the call." />; }
