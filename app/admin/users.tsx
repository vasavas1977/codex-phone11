import { UnavailableAdminScreen } from "@/components/admin/unavailable-admin-screen";

export default function AdminScreen() {
  return (
    <UnavailableAdminScreen
      title="People & extensions"
      description="People and extension management needs a live workspace directory before it can be used safely."
    />
  );
}
