# Phone11 meetings: Zoom-informed UX and release boundary

Phone11 uses its own brand and Connect11 meeting service. Zoom Workplace is the interaction reference, not an SDK or a source of graphics. These references describe the behavior reviewed on 25 September 2026:

- [Zoom mobile meeting controls](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0063582): primary controls at the bottom, participant panel and More for secondary actions.
- [Zoom meeting join flow](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0060732): review audio and video before joining and distinguish entry from waiting-room state.
- [Zoom participant controls](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0062674): connected-audio state, mute, video, participants and leave; availability varies by role and policy.
- [Zoom desktop meeting layouts](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0063672): gallery and speaker layouts keep participant identity and media controls distinct.
- [Zoom account meeting settings](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0061300) and [waiting room](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0059359): account policy must be enforced server-side, with explicit admin/user scope.
- [Zoom tiered settings](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0065579): account, group, and user values have explicit precedence; locked account or group settings cannot be changed below that level.
- [Zoom iOS audio](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0064460): joining meeting audio is a distinct step from muting an already connected microphone.

## Interaction parity sequence

Phone11 keeps its own visual identity and copies the useful interaction model, not Zoom artwork or labels. The order below follows the user-visible meeting journey and keeps controls honest about the capabilities the service actually supports.

| Moment | Expected Phone11 behavior | Current boundary |
| --- | --- | --- |
| Before joining | Show the chosen meeting, signed-in identity, camera and microphone choices, and an unmistakable Join action. Keep Back available. | Mobile shows identity and media choices without capturing before join. Desktop has an opt-in, video-only local camera preview; audio-device testing remains unimplemented. |
| Connecting | Distinguish joining, connected, reconnecting, and failed states. Never show a working mute control before media is connected. | Implemented in the mobile room; real-device network transitions remain unverified. |
| During mobile meeting | Keep the other person's video prominent and primary audio, video, participants, and leave controls reachable at the bottom. Put secondary actions in More. | Implemented for admitted calls. Meeting chat, share, hand raise, and captions must not be offered until functional and authorized. |
| During desktop meeting | Show named video tiles, a participant roster, gallery/speaker selection, audible output status, and persistent mute, camera, and leave controls. | Real join, media controls, roster, and gallery/speaker layouts are implemented. Names are shown when supplied by the service; the current plain-video contract does not yet provide an authoritative name mapping and otherwise displays “Participant”. Live admission and macOS/Windows media acceptance remain. |
| Admin setup | Owner/admin sees workspace meeting eligibility and channel host permissions. Future account/group/user policy must show inheritance and lock state only when enforced by server admission and room actions. | Channel host permission is live for the admitted Phone11 workspace. Waiting room, recording, sharing, and policy inheritance are not live settings. |

The iPhone audio acceptance gate is concrete: join one room on extensions 3001 and 1020 using the same signed build, unmute both, and hear speech in both directions. Video frames alone do not pass this gate. A failed direction should be diagnosed from the exact device log and audio route before adding more controls.

The interaction model is grounded in Zoom's current support guidance: [starting from a Team Chat conversation](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0059918) invites group or channel members; [mobile meeting controls](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0063582) keep primary audio/video controls in the bottom toolbar and secondary actions in More; [prejoin preview](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0061118) makes camera state explicit; and [tiered portal settings](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0061300) distinguish user controls from admin-locked settings. Phone11's optional member deselection is an added choice requested for its channels. These sources define design behavior, not evidence that every Zoom feature is implemented here.

## Implemented source slice

| Surface        | Behavior                                                                                                                                                                                                                           | Evidence boundary                                                                                  |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| iPhone meeting | Prejoin shows chosen mic and camera states; room keeps video prominent, bottom media/participants/more/leave controls, live roster and connection status. Only existing session actions are offered.                               | Source and component tests; a new signed build and real two-phone usability test are required.     |
| Desktop trial  | Signed-in Meetings entry opens a sandboxed, isolated media window. Privileged code fetches admission for the phone tenant; the calling renderer receives no meeting token. The media window provides room choice, prejoin, join/leave, microphone and camera controls, and incoming audio/video. SIP activity closes the meeting through a media-stop barrier. | Source, IPC/security and local build tests passed. A macOS arm64 package from committed source passed ad-hoc code-signature and helper-integrity verification; distributable signing, Windows package/runtime, and two-party media tests remain. |
| Admin portal   | Tenant-scoped Meetings page lists eligible group/channel members and lets an active owner/admin set `can_start_meeting`. The same flag is checked by channel-meeting start. Disabled tenant mapping or absent schema fails closed. | Live signed-in owner view showed the Test channel with both 3001 and 1020 enabled. A fresh permission mutation and another-tenant browser check remain untested. |

## Next service gates

Desktop admission and an isolated LiveKit window are implemented in source. The controlled entry is `meetings.availableForTenant` plus a tenant-bound `meetings.join`, and desktop must reject any room that does not match the signed-in phone tenant. The calling renderer remains isolated from media and credentials. Audio-device selection, reconnect quality, signed macOS/Windows packaging, and a real two-party A/V test are still release gates.

The desktop roster/layout source passed 17 desktop tests, typecheck and build at `fb63d62`. Its refreshed macOS arm64 trial package is staged at `/Users/vasavas16macbookpro/Library/Application Support/Phone11/desktop-meeting-packages/roster-fb63d62/Phone11-Desktop-Trial-darwin-arm64/Phone11-Desktop-Trial.app`; the packaging gate verified its committed source, Siprix helper integrity and ad-hoc local code signature. It has not replaced the running desktop trial. The live API now recognizes `meetings.availableForTenant`; desktop runtime admission and media still require a separate test.

The admin page must not display waiting-room, recording, screen-sharing or policy-lock switches until Connect11/Phone11 enforce each setting on token issuance and in-room actions. A UI toggle alone cannot define access. Channel host permission is the first supported setting; it does not grant meeting access outside an admitted channel or create an arbitrary room.

Meeting acceptance requires two signed native iPhones for camera frames, speaking/hearing in both directions, mute/unmute, camera on/off, participant roster, permission denial, disconnect/reconnect, SIP interruption, sign-out, and leave/rejoin. Build 96 from source `eba9a3c037c888634b19ad33498831720768db87` passed the signed IPA verifier and was installed on the paired iPhone 17 Pro Max and iPhone 15 Pro Max; device inventory reports version 1.0.0 build 96 on both. Its two-way listening and UI behavior are not yet confirmed. Record exact build/source IDs and device result separately from test success. A third participant is needed before claiming group layout or three-party media acceptance.

## Independent review follow-up

Desktop shared-screen video is now mounted separately from participant cameras, with `object-fit: contain` and SDK attachment cleanup on replacement, unpublish and leave. Camera-off participants can keep sharing without their screen being removed by roster updates. Missing display names use “Participant”, never opaque provider identity; completing an authenticated name mapping remains a service contract gap. This update passed 19 desktop tests, TypeScript and the Electron build locally. A refreshed macOS arm64 package from commit `a3eea23` is staged at `/Users/vasavas16macbookpro/Library/Application Support/Phone11/desktop-meeting-packages/media-a3eea23/Phone11-Desktop-Trial-darwin-arm64/Phone11-Desktop-Trial.app`. Committed-source, helper-integrity and deep strict ad-hoc signature checks passed. It is not installed and is not a notarized distributable.

The integrated backend candidate preserves the deployed desktop provisioning overlay and includes the tenant-first admission lock change. Its 90 local meeting tests, TypeScript and backend bundle pass. Independent review found and closed an extension-revocation race. Tenant-scoped assignment and extension locks now preserve eligibility through lease confirmation. A disposable PostgreSQL 17 concurrency test passed for concurrent joins, start/admin contention, cross-tenant ordering and revocation; its process was stopped. This is local database proof, not hosted deployment. The source fix is integrated in the main branch and the provisioning-preserving backend candidate.

## Prejoin and trusted-name follow-up

Desktop commit `59d0bcd` adds an optional local camera preview that starts off. Turning it off, joining, signing out, SIP interruption, and leaving stop or drain preview acquisition; late permission results cannot retain an orphan stream. Mobile commit `3354cbf` clarifies signed-in identity and media choices. Independent review found no concrete P0–P2 in those changes. An unresolved OS permission prompt can delay join while acquisition settles; physical-device behavior remains a release check. Commit `8fe1f43` polishes the desktop label and camera placeholder.

Phone11 commit `3e50c252c27465426be5088343718ee08bdd2bff` adds the server-only, per-tenant `displayNameEnabled` opt-in, disabled by default. It reads an authorized directory name during admission, normalizes and validates the label, and preserves the opaque participant identity. Public join requests cannot supply names. Independent source review found no concrete P0–P2; 94 meeting-server tests passed, with the separately configured PostgreSQL concurrency test skipped in that run. Desktop's 23 tests, mobile's four prejoin tests, and both TypeScript checks passed. Connect11 has not implemented the optional `display_name` contract, so this flag must remain disabled.

That earlier `meetings.availableForTenant` probe returned HTTP 404 and the first SSH method was rejected. A later controlled backend rollout on 25 September 2026 superseded those observations: the route now returns HTTP 401 for anonymous callers, the PostgreSQL 16.13 meeting schema was checked read-only, and the prior backend remains healthy for rollback. The authenticated admin and channel-picker browser checks below establish a narrower live result; they do not establish handset media.

The reproducible `desktop/app` command `npm run test:meeting-preview` was committed in `f8629a0` and independently rerun successfully. It loads the actual prejoin UI/preload with a synthetic Chromium video device and a refused-admission stub in a temporary isolated session. It verifies default-off capture, video-only opt-in, toggle cleanup, tracks stopped before admission, and leave acknowledgement after cleanup. Network requests are blocked; it is not real-camera or meeting proof.

A refreshed macOS arm64 trial from committed source `d911a05` is staged at `/Users/vasavas16macbookpro/Library/Application Support/Phone11/desktop-meeting-packages/prejoin-d911a05/Phone11-Desktop-Trial-darwin-arm64/Phone11-Desktop-Trial.app`. Packaging verified committed source, helper integrity before/after signing, and deep strict ad-hoc code signing. The existing app was not replaced. This local package is not notarized and does not establish Windows or live meeting acceptance.

## Live backend and web meeting surface — 25 September 2026

The protected meeting backend is active on healthy loopback port 3008; the previous backend on 3007 remains healthy. Public anonymous `meetings.availableForTenant`, `meetings.adminOverview`, and `POST meetings.join` return 401 with recognized procedure paths. The guarded tRPC route rollback receipt and exact image/bundle pins are in [MEETING-ADMIN-BACKEND-CANDIDATE-20260925.md](MEETING-ADMIN-BACKEND-CANDIDATE-20260925.md). This proves route exposure and authentication, not a successful join.

The static web export from exact application source `47110c55d148602b3c64c6d98b73839af734144d` is active at `https://1toall.phone11.ai`. All 1,862 tracked source blobs matched the clean archive before export. Its export manifest SHA-256 is `41bd1fddca0f3622a8b553f21a3e235ef4aac96edad2e314552f526f170cd3c0`; release marker SHA-256 is `548de6306f7153770e941a7e74af0f4324ec1a3f27b97285f5ad419794a7b91d`; main browser bundle is `_expo/static/js/web/entry-64d9ab648361e65b1d9c29247f2e82d7.js`, SHA-256 `df24bc5f3ca427935e2109d4fbd51d3155a77530533aae8422380f10c25933ca`. The guarded static operator pin update is commit `85521bf`; its 22 tests passed, as did 33 focused meeting/admin UI tests, TypeScript and the clean web export. An independent source review found no P0–P2 blocker.

The first static activation failed its web probe because the candidate files were root-readable but not Nginx-worker-readable. Its automatic rollback restored the exact prior `1de803b` link, Nginx hashes, and public marker. The failed receipt was preserved as a recovered operation. After setting only the inactive candidate tree to root-owned, world-readable `0555` directories and `0444` files, the web-worker read check, prepare, dry run, and guarded activation passed. The active receipt is `/etc/nginx/phone11-static-portal-rollout/47110c55d148602b3c64c6d98b73839af734144d/receipt.json`, SHA-256 `e46232673f0076bf49988868806312dd109832d647b0084f977b490d9a0d9f6b`. Nginx site and full-config hashes remained unchanged. The public marker and browser-bundle bytes match the pinned hashes; `/admin/meetings`, `/teamchat`, `/auth/sign-in`, and the root return 200, while same-origin `/api/phone11-static-portal-probe` remains 404.

In the existing signed-in account 3001 browser session, the Test channel displayed **Start channel meeting**. Its picker loaded the eligible 1020 member selected by default, allowed deselection, and disabled Start with zero selected. The Meetings admin page loaded two eligible Test-channel members and showed both 3001 and 1020 as hosts. No meeting was started and no host permission was changed in those checks. Ordinary-user/other-tenant denial, a live join, two-device audio, and native iPhone/desktop usability still need acceptance.

Guarded static rollback, if required on the edge host:

```sh
sudo python3 /opt/phone11ai/portal-operator/47110c55d148602b3c64c6d98b73839af734144d/phone11-static-portal-rollout.py --rollback --manifest /root/phone11-static-portal-47110c5.json
```

The staged desktop trial shares a bundle ID and user-data profile with the running calling app, so it has not been launched alongside that app. Use an isolated macOS account or test machine for desktop media acceptance. Windows packaging and runtime proof remain outstanding.

## Meeting-bot coordination

An isolated shared-bot UI fix is committed at `5661a87c351770daa1d3792c38026e296e256ad8` on `work/meeting-bot-intent-status-20260925` (base `28a505f`). It distinguishes durable `intent_recorded` from accepted capture and preserves exact-request retry/cancel and stale-response checks. English/Thai copy does not claim the bot joined or recorded. Its 40 reducer tests and TypeScript check passed; independent source review found no concrete P0–P2. The **Complete Super Number v7.2 Alpha** task integrated the four-file slice into draft [PR #369](https://github.com/vasavas1977/note11-your-ai-meeting-companion/pull/369) at `d3218da`, based on Alpha `21187e7`. That task reports 40 focused and 186 shared-conversation tests plus TypeScript passing; CI/build status is separate. It is not installed in Phone11 or activated.

**Complete Connect11 project** verified at local source `70734db4779351c4dfb554abf285b66fb89ef100` that protected `phone11-plain-video.v1` has no native recording, transcript, meeting-summary, or display-name API. Generic voicebot dispatch is not a meeting-notetaker contract and must not bypass protected admission. Phone11 bot/recording capabilities remain unavailable. The next service slice needs an explicit tenant-scoped start/stop/read contract, participant consent/withdrawal behavior, visible capture state, storage/retention ownership with Note11, and provider arrival/stop evidence. No runtime capture, billable agent dispatch or production change was performed.
