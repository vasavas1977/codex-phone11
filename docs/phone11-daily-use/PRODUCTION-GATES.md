# Phone11: long-call licensing and background incoming-call commissioning

Reviewed 10 September 2026 against official vendor documentation and repository source. This document separates missing implementation from provider/account configuration and physical acceptance. It contains no keys and does not report a purchase, vendor contact, APNs activation or production readiness.

The working preview remains an audio-only, single-call app. A successful foreground call does not prove locked/background delivery. The current trial has a 60-second call limit; the new optional license configuration described below has not been exercised with a purchased valid license.

## Siprix commercial prerequisite and prepared implementation

Siprix's sales page describes a one-time fee, selected platforms/features and three months of included technical assistance and updates; six- and twelve-month support options are offered. The page provides a quote form rather than a published numeric price. Platforms include iOS and Android, and the form distinguishes video, multiple calls and multiple accounts. The license is granted to the purchasing company and is non-transferable. These are the vendor's published terms, not a completed Phone11 quotation. [Official purchasing page](https://www.siprix-voip.com/contact-sales/)

A concrete quote brief, ready for the owner to use later: Phone11 is a React Native business phone app with a native Siprix bridge, initially shipping iOS voice calling and subsequently Android. Quote iOS voice now and an iOS+Android option, explicitly identify whether consultation/attended transfer and call waiting require the multiple-call option, and quote three versus twelve months of updates/support. Confirm production redistribution, existing pinned SDK compatibility, license delivery/rotation, and any application identifier requirements. There is no reason to buy video merely because the current interface once displayed a video button. No form or email has been submitted.

| Gate | Exact current state | Remaining work / evidence |
| --- | --- | --- |
| L1: commercial license | No purchased production license is established in this task. The vendor API says missing/expired credentials lead to trial mode and duration limits. [Siprix API](https://docs.siprix-voip.com/rst/api.html#module-init-attributes-functions) | Owner-approved quote and the correct company/platform/feature entitlement, followed by the vendor's actual license credential. Do not infer price or entitlement from the trial. |
| L2: native configuration | **Prepared in this task:** `app.config.ts` reads non-public build variable `PHONE11_SIPRIX_LICENSE` inside a `withInfoPlist` prebuild mod. It writes native plist key `Phone11SiprixLicense` only for Siprix builds. Missing/blank values remove stale plist values. | Configure the build environment later through its protected secret mechanism. Never put the value in Git, `EXPO_PUBLIC_*`, `extra`, logs or chat. An embedded native plist value is part of the distributed binary; it is not a server-side secret vault. Verify the vendor permits the chosen delivery method. |
| L3: native SDK application | **Prepared:** `modules/phone11-siprix/ios/Phone11Siprix.m` reads the native plist value and sets `SiprixIniData.license`. JavaScript still calls `initialize({})`; nonempty JS initialization options remain rejected. Blank/non-string native values leave trial behavior unchanged. | A real credential must be accepted by the actual SDK. Existing SDK errors and `onTrialModeNotified` remain authoritative; presence of a string does not assert validity. No SDK binary version was changed. |
| L4: feature scope | Native initialization still uses `singleCallMode = YES` and `enableVideoCall = NO`. Transfer and second calls remain disabled in the adapter/UI. | Buying an option does not implement it. Future consultation, transfer and video need bridge/event/state changes plus physical tests before their controls can return. |
| L5: production acceptance | Configuration tests passed with fake values; exact staged SDK-header compilation and native bridge assertions passed. No real-key or long-call acceptance was performed. | Signed production-licensed build, unchanged identity/provisioning, no trial notification, and successful calls beyond the old cutoff. Keep preview wording until this evidence exists. |

The optional path was tested by evaluating the actual Expo native plist mod with absent, blank and fake credentials and verifying that the fake value is absent from **public Expo runtime configuration**. Directly assigning the key to `ios.infoPlist` in the exported Expo config would expose it through that runtime config; the prebuild mod avoids that path. Native tests preserve the default `nil` license and exercise trimming/forwarding a fake string. These tests verify plumbing, not vendor authorization.

## Background incoming calls: engineering still missing

Apple's model uses a native `PKPushRegistry` token, APNs delivery, immediate CallKit reporting and then connection to the call service. Ordinary Expo notification tokens are not VoIP PushKit tokens. [Apple PushKit overview](https://developer.apple.com/documentation/PushKit)

The repository currently has CallKit audio/call handling, authenticated push-registration/trigger boundaries and truthful provider failures. It does **not** have a commissioned end-to-end background call path.

| Gate | Repository evidence | Required implementation / contract |
| --- | --- | --- |
| P1: native token lifecycle | **Prepared, disabled candidate:** `Phone11VoipPush.m` implements PushKit token rotation/invalidation and native stop; the shipping compile gate is zero, so no registry is created. `lib/push/client.ts` binds only the authenticated enabled phone owner and preserves pending revocations in device-only encrypted storage. | Native app-launch bootstrap and complete SIP wake must precede opening the gate. The callback immediately reports a generic unavailable call through the existing RNCallKeep provider, then ends only that fresh UUID on successful report. It does not resume SIP or claim background calling. See [native foundation](NATIVE-PUSH-FOUNDATION.md). |
| P2: launch and CallKit correlation | Unexpected callbacks use RNCallKeep's public shared provider, a fresh UUID, and no payload caller data; host native tests prove no duplicate provider or existing-call termination. No Expo AppDelegate PushKit bootstrap is installed. | Implement native cold launch, authenticated push/SIP transaction correlation, Answer deferral, caller-cancel/expiry handling and complete provider delegate handoff. This candidate deliberately cannot answer a pushed call. |
| P3: call identity and cancellation | Existing app call IDs are SDK-local; gateway payload currently contains `callId`, caller fields, video flag and timestamp. There is no proven push-to-SDK-call correlation protocol. | Define `{serverCallId, callUUID, owner/device binding, expiresAt}`; map this to the actual SDK call ID only after its INVITE arrives. Deduplicate wake/SIP/native events. Before completing Answer verify the call is still live. Cancel/finish over the established signaling connection or a scoped call-status endpoint, not additional VoIP wake pushes. [Apple incoming VoIP handling](https://developer.apple.com/documentation/pushkit/responding-to-voip-notifications-from-pushkit) |
| P4: credentials while locked | `lib/sip/account-store.ts` stores SIP credentials with `WHEN_UNLOCKED_THIS_DEVICE_ONLY`. A cold launch while locked cannot assume that account material is readable. | Design a minimal, device-bound background credential/session path with explicit revocation, or a short-lived wake credential exchange. Decide whether a limited item may use after-first-unlock accessibility; do not blindly relax all account/session storage. Test reboot-before-first-unlock separately. |
| P5: durable device registration | **Second server candidate prepared:** `server/push/repository.ts` and add-only `migration.sql` persist auth-session/owner/tenant/extension/device/platform bindings, token rotation revisions and provider acceptance time in PostgreSQL. Fresh session, enabled identity and assignment are checked transactionally on registration and before delivery; deleting the auth session cascades its device rows. | Apply the reviewed migration to the approved database before deploying this candidate. Complete native invalidation and offline revocation commissioning; server persistence does not implement the native token lifecycle. Real local PostgreSQL isolation/concurrency tests pass. |
| P6: APNs transport | **Second server candidate prepared:** `server/push/apns.ts` uses `node:http2` over TLS, P256 ES256/P1363 JWTs, a 50-minute token cache, five-second network budget, bounded throttle retry, safe provider classifications and invalid-token cleanup. Topic/environment come from server configuration. | Configure the approved provider, deploy the candidate and prove actual APNs acceptance/native receipt. Connections are currently isolated per request and cleaned up; sustained-load connection pooling remains a later operational optimization. HTTP/2 mocks are not live Apple evidence. [Apple APNs connections](https://developer.apple.com/documentation/usernotifications/establishing-a-connection-to-apns) |
| P7: actual PBX wake routing | The checked-in Kamailio config forwards unregistered targets to literal `FLEXISIP_IP:FLEXISIP_PORT` placeholders. `push.triggerCall` exists behind `PUSH_SHARED_SECRET`, but source does not establish a deployed proxy integration. | Choose and implement one path: an actually commissioned Flexisip service or the custom gateway. The proxy must authenticate the wake request, keep the correct INVITE pending for a bounded interval, resume toward the refreshed contact, and stop on caller cancellation/timeout. Handle stale registered contacts, not only missing location rows. The repo template is not current live-server proof. |
| P8: registration and media after wake | Foreground reconnect and CallKit audio-session integration have tests. There is no accepted suspended/cold-wake call sequence. | Wake → correct owner account → registration/contact readiness → matched INVITE → native Answer → media activation → clear two-way audio → real termination. Avoid reconnecting into another signed-in workspace and avoid duplicate history rows. |
| P9: chat notifications | Team Chat persistence is separate from VoIP calling; the generic notification engine uses Expo/device tokens and a separate Flexisip-shaped endpoint. | Implement standard APNs/FCM/Expo message notifications with their own token type and deep links. Never send chat messages or generic updates using VoIP push. [Apple VoIP push purpose](https://developer.apple.com/documentation/pushkit/pkpushtype/voip) |

## Provider/build commissioning after the code exists

This table lists names found in repository source; it neither reads nor verifies deployment values.

| Configuration | Meaning and required verification |
| --- | --- |
| `PHONE11_BUNDLE_ID` | Build identity in `app.config.ts`/`eas.json`. Use the actual signed application's `CFBundleIdentifier`; the preview profile overrides the default. Provisioning profile, APNs topic and device token must belong to that identity/environment. |
| Push Notifications capability / `aps-environment` | Verify Apple developer entitlement and the signed app's embedded entitlement. Existing `UIBackgroundModes` includes `audio`, `voip`, `remote-notification`; those declarations alone do not prove an APNs entitlement or registry implementation. |
| `APNS_KEY_PATH`, `APNS_KEY_ID`, `APNS_TEAM_ID` | Current server uses a `.p8` signing key path and ES256 token authentication. Mount the private key in protected server storage; configure the matching Apple account. This implementation supports token authentication, not a `.p12` provider-certificate path. |
| `PUSH_SHARED_SECRET` and `x-push-secret` | Authenticate the trusted proxy→gateway call trigger. Current middleware rejects missing/placeholder configuration. Configure the exact same protected value on the two services without exposing it to the mobile app. |
| `APNS_BUNDLE_ID`, `APNS_ENVIRONMENT` and token `sandbox`, `bundleId`, `tokenType` | The second server candidate requires one approved bundle ID and `production` or `sandbox`. Token metadata must match both and type must be `voip` before any Apple request. The client cannot choose a different provider host/topic. Do not substitute an Expo/ordinary alert token. |
| `APPLE_PUSH_CERT_PATH`, `APPLE_PUSH_KEY_PATH`, `APPLE_PUSH_BUNDLE_ID` | Names in `.env.example`, not the names read by the current custom APNs sender. Reconcile the example during commissioning; merely filling these legacy names will not configure `sendApnsPush`. |
| `FCM_PROJECT_ID`, `GOOGLE_APPLICATION_CREDENTIALS` | Separate Android HTTP-v1 push path. This is not part of iOS PushKit commissioning. The older example `FCM_API_KEY` does not configure the current HTTP-v1 sender. |

Use short-lived call notifications with expiration zero or only a few seconds and record provider response IDs/statuses without dumping tokens/payloads. APNs acknowledgment means Apple accepted the request, not that the phone rang. [Apple APNs request contract](https://developer.apple.com/documentation/usernotifications/sending-notification-requests-to-apns)

## Minimum acceptance before claiming daily-use production

1. **Long calls:** on a signed licensed build, place one incoming and one outgoing consenting test call on Wi-Fi and cellular. Each must cross 90 seconds and then ten minutes with clear two-way audio and working End call from either endpoint. Record SDK/build version, absence/presence of trial notification, actual duration and terminal reason. Test credential rejection explicitly without asserting that any string is valid.
2. **Background/locked calls:** use the actual assigned public number, not only direct echo. Repeat while app foreground, background for five minutes, and locked for five minutes, on Wi-Fi and cellular. Observe ring, Answer, intelligible audio, End, one Recents entry and no leftover server call. Repeat decline and no-answer.
3. **Cold start and interrupted signaling:** test OS-reclaimed app, reboot followed by first unlock and relock, and reboot before first unlock. Treat user force-quit separately and document observed OS behavior. Answer before SIP is ready; cancel from the caller before/after the wake; deliver duplicate/stale payloads; temporarily remove network. No ghost call or premature “connected” state may remain.
4. **Token/owner lifecycle:** rotate a token, restart the gateway, revoke the extension, sign out and change accounts/workspaces. Old bindings must not wake or reveal caller data to the next account. Confirm wrong-topic/environment failures produce a useful operational error and no fake success.
5. **Evidence chain:** keep a sanitized correlation from provider acceptance to native push callback, CallKit presentation, SIP/SDK session, actual audio and termination. Do not put credentials, raw tokens or full signaling captures in this public repository. The release record must distinguish code tests, deployment, provider acceptance and physical handset acceptance.

## What can be done without provider credentials

The optional native license path is now prepared and tested with fake values. Durable PostgreSQL token storage and explicit HTTP/2 APNs transport are now a tested second server candidate; see [the implementation record](SERVER-PUSH-CANDIDATE.md). They are not yet deployment/provider acceptance. The disabled native PushKit/token-binding foundation and best-effort pre-logout cleanup are now prepared with host native and owner lifecycle tests; authoritative server session revocation handles device-token deletion. Remaining engineering includes AppDelegate cold bootstrap, full CallKit/SIP correlation and resume, locked credential policy design, and proxy transaction wake/resume/cancel handling. Those can receive bounded simulator/unit/local integration tests before a real key is configured. Actual APNs delivery, a valid commercial SDK entitlement, signed-app entitlements and physical incoming/long-call acceptance remain later commissioning gates. None should be described as already complete merely because the preview build passes CI.
